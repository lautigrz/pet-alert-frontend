# Angular y @analogjs/vite-plugin-angular declaran peer deps que entran en
# conflicto, y eso hace que npm install entre en backtracking y se cuelgue.
# legacy-peer-deps evita ese cuelgue y mantiene install y ci en el mismo modo
# (las versiones son compatibles igual).
legacy-peer-deps=true
